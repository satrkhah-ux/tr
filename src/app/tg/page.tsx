import { TgEntry } from "@/components/traveliun/TgEntry";

/** Telegram Mini App entry — public route; auth happens via verified initData. */
export default function TgPage() {
  return <TgEntry />;
}
